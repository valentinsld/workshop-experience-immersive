@font-face
  src url('@fonts/Montserrat-Regular.ttf')
  font-family Montserrat

@import './common.styl'
@import './loading.styl'
@import './qte.styl'
@import './endScene.styl'

:root 
  --white : #fff

html {
  color var(--white)
}